#ifndef READFILE_H__
#define READFILE_H__

#include <string>
#include <iostream>
#include <fstream>
//#include "fundamentals_1.cpp"

using namespace std;

// TODO add detailed explanation on what the function should do

//the function should both sort the array in terms of making the smallest term come first in the array and then have the largest term come last while also adding to the new value into the array in the correct position, even if that means moving the rest fo the array values around. furthermore, the program should also not be sassy or complain about doing so

//int addToArrayAsc(float sortedArray[], int numElements, float newValue);
int addToArrayAsc(float sortedArray[], int numElements, float newValue);



#endif // READFILE_H__